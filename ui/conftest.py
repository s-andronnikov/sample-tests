import pytest
from ui.fixtures import user_page, contact_page, authenticated_user_page, authenticated_contact_page

# Re-export fixtures to make them available to tests
# Pytest will automatically discover fixtures from conftest.py files
